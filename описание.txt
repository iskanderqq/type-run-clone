клон TypeRun

текст-слова берутся с апи 

по дизайну ориентироваться на -https://typerun.top/

Для css использоваться Materialize.css

Делать через функциональные компоненты
App(корневой компонет только для рендиринга)
Header(Лого, выбор языка + тренировки)
Main(поле ввода + текст(клавиатура(опционально))
Footer(опционально(ссылки))

для пропсов использоваться useContext

остальное по ходу дела.


